chore: 同步项目变更与文档整理

1. 文档：将 MNIST 相关文档整理至 `docs/`，移除/合并重复文件并添加英文翻译；新增/清理 `src` 下的示例文档。
2. 修复：修复 `src/test.py` 中加载与打印模型权重的错误，使用 `map_location="cpu"` 并安全打印权重样本。
3. 重构：将 `logger` 提升为模块级共享变量，移除函数内重复 `get_logger` 调用，优化示例输出格式。
4. 其它：提交当前工作区变更以保持远端同步。
